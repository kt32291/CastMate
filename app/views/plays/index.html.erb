


<!-- <div ng-controller="playsCtrl">
  <ul>
    <li ng-repeat="play in plays">
      {{play.id}}
      {{play.title}}
    </li>
  </ul>

  <textcomplete members='plays' message='play'></textcomplete>

  {{ play }}

</div> -->

<input type="text">
<table>
  <% @plays.each do |play| %>
  <tr>
    <td><%= link_to play.title, play %></td>
    <td><%= play.type_of_show %></td>
  </tr>
  <% end %>
</table>
